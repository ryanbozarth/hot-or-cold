# Three Functions
-  <code>newGame()</code>
-  <code>checkGuess()</code>
-  <code>feedbackGuess()</code>

##<code>newGame()</code>

function myFunction() {
    var x = Math.floor((Math.random() * 100) + 1);
}

display “make your guess” as h2
clear out input 
clear guess #
clear previous guesses
